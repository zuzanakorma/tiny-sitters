type SitterType = {
    _id: string;
    name: string,
    dateofbirth: string,
    description: string,
    weekdays?: boolean,
    weekends?: boolean,
    image: string
}

export default SitterType;